#include<stdio.h>
int main(){
    /* A Programe to check for user given
     character are digit or not*/
    int a = 9;
    int print = (a==1 || a<=9);
    printf("%d", print);
    return 0;
}